name: Backend CI

on:
  push:
    paths:
      - '**/*.go'
      - 'go.mod'
      - '.golangci.yml'
      - 'Dockerfile'
      - 'requirements-ytdlp.txt'
      - '.github/workflows/backend.yml'
      - '.github/workflows/docker-multiarch.yml'
  pull_request:
    paths:
      - '**/*.go'
      - 'go.mod'
      - '.golangci.yml'
      - 'Dockerfile'
      - 'requirements-ytdlp.txt'
      - '.github/workflows/backend.yml'

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      # gofmt is not among golangci-lint's default linters, so formatting
      # drift went unnoticed until someone happened to run gofmt locally.
      - name: Check formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-formatted. Run: gofmt -w ."
            echo "$unformatted"
            exit 1
          fi
      - name: go vet
        run: go vet ./...
      - uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Run tests
        run: go test -race ./...

  build:
    name: Build & Health Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Build
        run: CGO_ENABLED=0 go build -o /tmp/backend ./cmd/server
      - name: Health check
        run: |
          DECK_STORAGE_PROVIDER=local PORT=18080 /tmp/backend &
          for i in $(seq 1 15); do
            sleep 1
            curl --silent --fail http://localhost:18080/health && kill %1 && exit 0
          done
          kill %1
          exit 1
